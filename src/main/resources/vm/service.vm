package ${packageName}.service;

import ${packageName}.entity.${domainName}Example;
import ${packageName}.entity.${domainName};
import ${packageName}.form.${domainName}Form;
import com.anso.utils.base.BaseService;

import java.util.List;

/**
* @author ${auth}
* @Description:${domainNameDesc}controller
* @date ${time}
*/
public interface ${domainName}Service extends BaseService<${domainName},${domainName}Example> {

    void edit(int id ,${domainName}Form userForm);

    int save(${domainName}Form userForm);

    void delete(int id);
}